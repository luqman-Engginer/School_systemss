# Base URL backend API. Kosongkan = same-origin (default, cocok untuk dev via Vite proxy).
# Di Vercel, set VITE_API_URL pada project settings lalu rebuild.
# Contoh: VITE_API_URL=https://schoolhub-api.example.com
VITE_API_URL=